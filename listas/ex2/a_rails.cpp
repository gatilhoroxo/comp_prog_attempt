#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    stack<int> estacao;
    int n=1;
    cin >> n;
    do {
        int v=0;
        while(true){
            cin >> v;
        }
        cin >> n;
    } while(n);

    return 0;
}