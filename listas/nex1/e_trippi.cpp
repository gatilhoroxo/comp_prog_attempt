#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; 
    cin >> t;

    char c;
    while(t--){
        string pq, r, s;
        cin >> pq >> r >> s;
        cout << pq[0] << r[0] << s[0] << '\n';
    }

    return 0;
}