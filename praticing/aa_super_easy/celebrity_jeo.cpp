//UVA - 1124
//é muito besta esse //foi já
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    string linha;
    while(getline(cin, linha)){
        cout << linha << '\n';
    }

    return 0;
}