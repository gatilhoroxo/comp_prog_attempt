//
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n, k; cin >> n >> k;
    vector<int> vt(n);
    for(auto&i:vt) {cin>>i;}
    long long soma=0;

    

    return 0;
}